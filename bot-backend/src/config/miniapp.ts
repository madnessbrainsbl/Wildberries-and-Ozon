// Mini App configuration
export const MINIAPP_CONFIG = {
  // For local testing, we'll use a static HTML page
  url: process.env.MINIAPP_URL || 'http://localhost:5173',
  
  // Alternative: Use a pre-built static version hosted on GitHub Pages or similar
  // url: 'https://your-github-username.github.io/miniapp-store/'
};
